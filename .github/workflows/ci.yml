name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: matt-riley/matt-riley-ci/.github/workflows/node-ci.yml@v1
    with:
      package-manager: pnpm
      run-fallow: false
      run-knip: false
      run-lint: true
      run-test: true
      run-build: true
